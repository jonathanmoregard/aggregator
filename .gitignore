credentials/
.env
*.db
raw/
ingest.log
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

*.db-wal
*.db-shm

sync.log

# Timestamped backups tooling drops next to the file it rewrites
# (tasks/mission.md.bak-*). Ephemeral, never cleaned up — left untracked they
# made every autosync run report a dirty worktree forever.
*.bak-*

# Claude Code machine-local state. settings.local.json is per-machine
# permissions CC rewrites on its own; the lock is per-run. Without this,
# sync-agent.sh commits both on its next run.
.claude/settings.local.json

# Runtime lockfiles only — flake.lock and uv.lock are tracked source.
*.lock
!flake.lock
!uv.lock

# arftl per-run state (repo-autosync, 2026-08-13)
tasks/arftl-run.json

# Stage output the verifier redirects per run. They exist only while a gate is
# in flight — the script deletes them at the end — but the sync-agent cron
# auto-commits untracked files and does not know that, so a run that overlaps
# the cron publishes a test log to a public repo. Exit codes are read directly
# from these, never through a pipe, so they cannot become /dev/null.
/pytest.out
/ruff.out
/nixflake.out
/hygiene.out

# `nix build` drops this symlink into the repo root, and the verifier's
# `nix build --rebuild .#checks…aggregator-embed-unit-hygiene` runs on every
# gate — so it reappears after each one. Untracked, it is exactly what the
# sync-agent cron auto-commits: a dangling /nix/store pointer published to a
# public repo and broken on every other machine. Anchored so a source file
# legitimately named `result` further down the tree stays tracked.
/result
/result-*

# The dev shell's isolated XDG_DATA_HOME (flake.nix devShells.default
# shellHook). Everything a `uv run aggregator …` inside `nix develop` would
# otherwise have written to the LIVE cache lands here instead: cache.db and
# its -wal/-shm, the retrieval eval DB, the TickTick backup archive.
#
# Three reasons this must be ignored, not merely untracked:
#   1. cache.db is multi-GB in normal use. `nix develop` snapshots a dirty git
#      tree into the Nix store, and untracked-but-unignored files are part of
#      that snapshot — so without this line, entering the shell would copy the
#      developer's whole database into /nix/store, every time it changed.
#   2. The daily sync-agent.sh cron auto-commits untracked files. This repo is
#      public, and the cache contains scrubbed-but-personal history.
#   3. `*.db` above already covers the database itself, but not the directory,
#      the archive tree, or anything a future call site puts under
#      $XDG_DATA_HOME. Anchored at the root so the whole subtree is covered.
/.devshell-data/

# Working notes live in userspace, NOT in this repo (2026-08-16).
#
# These are agent/operator notes — mission state, durable user directives,
# handover primers, blocked-on-a-human items, cron output. They are personal
# working material, not source, and this repo is public. They were published
# once by accident; each entry below is a symlink into
# ~/.claude/tasks/aggregator/ so every existing path still resolves while the
# content stays out of git.
#
# A daily cron runs sync-agent.sh, which auto-commits and pushes anything it
# finds — so an un-ignored note file here is not a risk of publication, it is
# a guarantee of it.
tasks
tasks/
HANDOVER.md
pending_for_human.md
sync.log
